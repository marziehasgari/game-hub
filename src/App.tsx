import React from "react";
import { Button, ButtonGroup } from "@chakra-ui/react";
import "./App.css";

const App: React.FC = () => {
  return <Button colorScheme="blue">btn</Button>;
};

export default App;
